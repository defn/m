#ManagedBy: cue

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: coder
  annotations:
    cert-manager.io/issuer: zerossl-production
    external-dns.alpha.kubernetes.io/hostname: coder.defn.run
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: coder
                port:
                  number: 80
  tls:
    - hosts:
        - coder.defn.run
      secretName: coder.defn.run
